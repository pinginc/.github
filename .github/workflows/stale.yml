name: Close stale PRs

on:
  schedule:
    - cron: 0 0 */2 * * # Runs every other day at midnight UTC
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: pinginc/actions/.github/actions/stale@main
